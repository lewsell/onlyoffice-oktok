version: '3.3'
services:
  alist:
    restart: always
    volumes:
      - '/etc/alist:/opt/alist/data'
    ports:
      - '80:80'
    environment:

    container_name: onlyoffice
    image: 'onlyoffice/documentserver'
